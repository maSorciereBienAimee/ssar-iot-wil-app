apiVersion: v1
kind: Service
metadata:
  name: wil-app-service
  namespace: dev
spec:
  type: LoadBalancer
  selector:
    app: wil-app-label
  ports:
    - protocol: TCP
      port: 8888
      targetPort: 8888

